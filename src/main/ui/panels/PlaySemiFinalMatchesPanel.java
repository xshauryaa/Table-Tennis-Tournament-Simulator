package ui.panels;

import javax.swing.*;

import model.Match;
import ui.StyleGuide;
import ui.TableTennisTournamentSimulatorApp;

// Represents the panel where user simulates semi final matches
public class PlaySemiFinalMatchesPanel extends JPanel {
    
    // EFFECTS: draws the panel where semi final matches are played
    public PlaySemiFinalMatchesPanel(TableTennisTournamentSimulatorApp owner, SideMenuPanel smp) {
        // stub
    }

    // EFFECTS: makes a label for "Semi Finals" and adds it to the screen
    private void addTitleLabel() {
        // stub
    }

    // EFFECTS: makes a JButton to simulate matches and adds it to the screen
    private void addSimulateButton(SideMenuPanel smp) {
        // stub
    }

    // EFFECTS: creates the panel for displaying all the matches in the semi finals
    private void addMatchDisplay() {
        // stub
    }
}